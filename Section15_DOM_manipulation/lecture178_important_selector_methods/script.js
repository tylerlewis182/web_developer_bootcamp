//alert('.js file linked');

// Get element by ID
var tag1 = document.getElementById("highlight")

// Get elements by Class
var tags1 = document.getElementsByClassName("bolded")

// Get elements by TagName
var tags2 = document.getElementsByTagName("ui")

// Get element by
var tag2 = document.getElementById("highlight")



/* --------------------------------------- */

// querySelector (takes a css style selector)
var tag = document.querySelector(".bolded"); // only returns the first element that matches the selector

// querySelectorAll (takes a css style selector)
var tags = document.querySelectorAll(".bolded"); // returns all elements that match the selector


